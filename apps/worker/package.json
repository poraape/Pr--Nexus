{
  "name": "@pr-nexus/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../../infra/wrangler.toml",
    "deploy": "wrangler deploy --config ../../infra/wrangler.toml",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pr-nexus/agents": "workspace:*",
    "@pr-nexus/contracts": "workspace:*",
    "@pr-nexus/schemas": "workspace:*",
    "@pr-nexus/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241018.1",
    "typescript": "~5.8.2",
    "wrangler": "^4.45.0"
  }
}
