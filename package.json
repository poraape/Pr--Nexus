{
  "name": "pr-nexus-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter @pr-nexus/web dev",
    "dev:worker": "pnpm --filter @pr-nexus/worker dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck"
  }
}
